name = "zan-site"
compatibility_date = "2026-07-01"

# Static-assets-only Worker (no server code needed).
# Cloudflare Workers free tier: 100,000 requests/day — plenty for a marketing site.
[assets]
directory = "./public"
not_found_handling = "single-page-application"
