using System;
using Gui;
using Gui.Widget;

/// EXAMPLES 面板：随发行版一起提供的示例工程树，双击在编辑器里打开示例
/// 源码，右键弹出面板自己的上下文菜单。
///
/// 界面在 `ExamplesPanel.zform` 里声明；这里只做数据绑定和事件接线。
partial class ExamplesPanel {
    List<TreeNode> shown;
    Input filter;

    void OnInit() {
        this.Tree.EmptyText(ZanIDE.T("No examples found.", "没有找到示例。"));
        shown = new List<TreeNode>();
    }

    void Wire(SignalInt sel, Input f) {
        filter = f;
        this.Tree.BindSel(sel).WithFilter(f);
        this.Tree.OnActivate(ZanIDE.ExampleActivated);
        this.Tree.OnContext(ZanIDE.ExampleContext);
    }

    List<TreeNode> Shown() { return shown; }

    TreeView TreeCtl() { return this.Tree; }

    void Render(App app, Rect area, List<TreeNode> nodes) {
        string q = "";
        if (filter != null) { q = filter.GetText(); }
        shown = ZanIDE.FilterNodes(nodes, q);
        this.Tree.Bind(shown);
        this.RenderInside(app, area);
    }
}
