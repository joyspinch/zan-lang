using System;
using Gui;
using Gui.Widget;

// Standard Zan GUI project entry for the IDE. The .zform owns the window
// layout; IdeShell composes the retained controls and application services.
partial class ZanIDE {
    static void OnLoad(Form form) {
        IdeShell.Attach(form);
    }
}
