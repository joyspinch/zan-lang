name = ZanIDE
type = gui
target = exe
entry = src/App.zform
assets = ide.css,ide_forms

# The IDE source tree is a normal GUI project. The repository build script adds
# the native GUI backend and toolchain files required for the shipped editor.
