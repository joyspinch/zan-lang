// sample Zan source lexed by zlex.zan
using System;

namespace Demo;

class Program {
    static void Main() {
        int x = 40 + 2;
        int y = x * 10;
        if (x >= 42) {
            Console.WriteLine("done");
        } else {
            Console.WriteLine(0);
        }
    }
}
