using System;
using Gui;

// Code-behind for {{NAME}}.zform.
// The window is designed visually in {{NAME}}.zform. The region
// between the ZAN-DESIGNER markers is generated from it - do not edit it;
// write your initialization and event logic in OnLoad.
class {{NAME}} {
    // <<< ZAN-DESIGNER BEGIN (generated from {{NAME}}.zform - do not edit) >>>
    // Generated from the visual design in {{NAME}}.zform. This whole
    // region is rewritten by the designer - do not edit it; put your own
    // initialization and event logic in OnLoad below.
    static void RenderDesign(App app) {
        int top = app.titlebarH;
        int fy = top + 24;
    }

    static void Main() {
        App app = App.CreateDark("{{NAME}}", 480, 520);
        app.Show();
        {{NAME}}.OnLoad(app);
        while (app.isRunning) {
            if (!app.ProcessEvent()) { break; }
            if (!app.needsRedraw) { continue; }
            app.BeginFrame();
            {{NAME}}.RenderDesign(app);
            app.RenderChrome("{{NAME}}");
            app.PresentFrame();
        }
    }
    // <<< ZAN-DESIGNER END >>>

    // Runs once after the window opens. Put your logic here.
    static void OnLoad(App app) {
    }
}
