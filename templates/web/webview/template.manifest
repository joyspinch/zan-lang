name=WebView Window
name.zh=WebView 窗口
category=WebView
icon=folder
kind=webview
type=gui
target=exe
entry=src/main.zan
order=80
desc=A window that hosts web content (needs a WebView runtime).
desc.zh=承载网页内容的窗口（需要 WebView 运行时）。
