using System.Data.Orm;

namespace ZanWeb.Model.Mall;

/* 版本频道的按版本发布记录：一次 channel 发布落一行，
 * 便于回滚（把 latest 指回旧行）与审计。 */
[Table(Name = "version_releases")]
class VersionRelease {
    [Column(IsIdentity = true)]
    int id;

    [Column(IsNullable = false)]
    int channelId;

    [Column(StringLength = 32, IsNullable = false)]
    string version;

    [Column(StringLength = 512)]
    string installerUrl;

    [Column(StringLength = 64)]
    string sha256;

    [Column(StringLength = 1024)]
    string signature;

    [Column(StringLength = 4096)]
    string releaseNotes;

    /* published / rolled_back */
    [Column(StringLength = 24, IsNullable = false)]
    string status;

    long createdAt;
}
