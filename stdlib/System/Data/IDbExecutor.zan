using System;

namespace System.Data;

/// <summary>
/// Minimal execution contract shared by database connections that support
/// synchronous parameterized SQL. Typed queries (`db.Query<T>()`) run
/// through this interface, so any implementing connection gets compile-time
/// entity mapping for free.
/// </summary>
interface IDbExecutor {
    DbResult Query(string sql, DbParams prms);
    int Execute(string sql, DbParams prms);
}
