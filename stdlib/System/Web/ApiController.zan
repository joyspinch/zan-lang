using System;

namespace System.Web;

/// <summary>
/// Base class for JSON API controllers. It has no routes of its own -- the
/// concrete API controllers live under src/controller/Api and derive from
/// this so they share the JSON response helpers of the rich base.
/// </summary>
class ApiController : Controller {
}
