using System;

namespace Gui;

/// GENERATED by scripts/scan_components.ps1 - do not edit by hand.
/// Registry of project-defined custom components discovered at build time.
class CustomComponents {
    static List<string> Kinds() {
        List<string> k = new List<string>();
        k.Add("Segment");
        k.Add("ThemeCard");
        k.Add("WindowShell");
        return k;
    }

    static string GroupOf(string kind) {
        if (kind == "Segment") { return "Editor"; }
        if (kind == "ThemeCard") { return "Editor"; }
        if (kind == "WindowShell") { return "Editor"; }
        return "";
    }

    static string IconOf(string kind) {
        if (kind == "Segment") { return "segment"; }
        if (kind == "ThemeCard") { return "palette"; }
        if (kind == "WindowShell") { return "window"; }
        return "";
    }

    static Control Create(string kind) {
        if (kind == "Segment") { return new Segment(); }
        if (kind == "ThemeCard") { return new ThemeCard(); }
        if (kind == "WindowShell") { return new WindowShell(); }
        return null;
    }
}
