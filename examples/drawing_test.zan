using System;
using System.Drawing;
using System.Windows.Forms;

class Program {
    static void Main() {
        Color red = Color.Red();
        Color blue = Color.Blue();
        int redRef = red.ToColorRef();
        Console.WriteLine("Red ColorRef: ");

        Rectangle r = Rectangle.Create(10, 20, 100, 50);
        if (r.Contains(50, 30)) {
            Console.WriteLine("Point is inside rectangle");
        }

        Point p = Point.Create(100, 200);
        Size s = Size.Create(640, 480);
        Console.WriteLine("Drawing test passed");
    }
}
