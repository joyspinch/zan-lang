// async/await test
class Program {
    static async int ComputeAsync(int x) {
        int result = x * 2;
        return result;
    }

    static void Main() {
        int val = await ComputeAsync(21);
        Console.WriteLine(val);
    }
}
