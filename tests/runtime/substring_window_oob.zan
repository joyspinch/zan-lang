// The two-argument Substring must reject a window running past the end
// (start + len > strlen): it used to memcpy whatever followed the string.
class Program {
    static void Main() {
        string s = "hello";
        string t = s.Substring(3, 5);
        Console.WriteLine(t);
    }
}
