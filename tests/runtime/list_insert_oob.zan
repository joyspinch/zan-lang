using System;
using System.Collections.Generic;
class Program {
    static void Main() {
        List<int> values = new List<int>();
        values.Add(1);
        values.Insert(values.Count + 1, 2);
    }
}
