// One-argument Substring with start past the end must trip the runtime
// check: it used to compute a negative length and memcpy off the heap.
class Program {
    static void Main() {
        string s = "hello";
        string t = s.Substring(99);
        Console.WriteLine(t);
    }
}
