// Conformance: integer text formatting is emitted as a digit loop instead of
// snprintf, so the boundaries the two lowerings could disagree on are pinned
// here: INT64_MIN (its magnitude does not fit in a positive long), values
// above INT64_MAX read as ulong, zero, narrow widths, and the {v:D}/{v:X}/
// {v:F} specs that still go through printf.
using System;

class Program {
    static void Main() {
        long min = -9223372036854775808;
        long max = 9223372036854775807;
        ulong umax = 18446744073709551615;
        ulong ubig = 9300000000000000000;
        int i = -7;
        short sh = -32768;
        byte b = 200;
        long zero = 0;

        Console.WriteLine($"{min}");
        Console.WriteLine($"{max}");
        Console.WriteLine($"{umax}");
        Console.WriteLine($"{ubig}");
        Console.WriteLine($"{zero}|{i}|{sh}|{b}");
        Console.WriteLine($"a{min}b{umax}c");

        Console.WriteLine(Convert.ToString(min));
        Console.WriteLine(Convert.ToString(umax));
        Console.WriteLine(Convert.ToString(zero));
        Console.WriteLine(Convert.ToString(i));

        Console.WriteLine(min.ToString());
        Console.WriteLine(umax.ToString());
        Console.WriteLine(zero.ToString());

        StringBuilder sb = new StringBuilder();
        sb.Append(min);
        sb.Append(",");
        sb.Append(max);
        sb.Append(",");
        sb.Append(0);
        Console.WriteLine(sb.ToString());

        // formatted specs keep the printf path
        Console.WriteLine($"{i:D8}");
        Console.WriteLine($"{255:X4}");
        Console.WriteLine($"{i:F2}");
    }
}
