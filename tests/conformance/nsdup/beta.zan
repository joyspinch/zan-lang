using System;

namespace Beta;

class Log {
    static string Line(string s) { return "beta: " + s; }
}

class Tag {
    string text;

    static Tag Of(string s) {
        Tag t = new Tag();
        t.text = s;
        return t;
    }

    string Text() { return "beta tag " + text; }
}
