using System;

namespace Alpha;

class Log {
    static string Line(string s) { return "alpha: " + s; }
}

class Tag {
    string text;

    static Tag Of(string s) {
        Tag t = new Tag();
        t.text = s;
        return t;
    }

    string Text() { return "alpha tag " + text; }
}
