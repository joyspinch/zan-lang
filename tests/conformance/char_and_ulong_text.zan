using System;

// A `char` is text, not a number: printing, writing and concatenating one
// yields the character itself (UTF-8 encoded), while an explicit `(int)` cast
// still yields its code point. A decimal literal above long.MaxValue keeps its
// unsigned value instead of saturating at long.MaxValue.
class Program {
    static char Ret() { return 'r'; }
    static string Wrap(char c) { return "<" + c + ">"; }

    static void Main() {
        char a = 'A';
        Console.WriteLine(a);
        Console.WriteLine(Ret());
        Console.WriteLine((char)(a + 2));
        Console.Write(a);
        Console.Write('!');
        Console.WriteLine("");

        Console.WriteLine("x=" + a);
        Console.WriteLine(a + "=x");
        Console.WriteLine("[" + a + a + "]");
        Console.WriteLine(Wrap('q'));
        Console.WriteLine($"i({a})");
        Console.WriteLine(String.Format("f={0}!", a));

        // Non-ASCII code points are encoded as UTF-8.
        char eur = (char)0x20AC;
        Console.WriteLine(eur);
        Console.WriteLine("e=" + eur);
        char n = (char)0x00F1;
        Console.WriteLine("n=" + n);

        // A cast to int is still the code point.
        Console.WriteLine((int)a);
        int code = a;
        Console.WriteLine(code);
        Console.WriteLine("c=" + (int)a);

        // Decimal literals past long.MaxValue stay unsigned.
        ulong max = 18446744073709551615;
        ulong big = 10000000000000000000;
        Console.WriteLine(max);
        Console.WriteLine(big);
        Console.WriteLine(max - big);
        long lmax = 9223372036854775807;
        Console.WriteLine(lmax);
        ulong hex = 0xFFFFFFFFFFFFFFFF;
        Console.WriteLine(hex);
    }
}
