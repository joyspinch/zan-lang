using System;
namespace Conf;
class Program {
    static void Main() {
        bool t = true;
        bool f = false;
        if (t && !f) { Console.WriteLine(1); }
        if (t || f) { Console.WriteLine(2); }
        if (!(t && f)) { Console.WriteLine(3); }
    }
}
