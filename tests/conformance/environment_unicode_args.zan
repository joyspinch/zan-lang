using System;

class EnvironmentUnicodeArgs {
    static void Main() {
        Console.WriteLine(Environment.ArgCount());
        Console.WriteLine(Environment.ArgAt(0));
        Console.WriteLine(Environment.ArgAt(1));
    }
}
