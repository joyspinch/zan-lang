// Declaring side of access_internal_module.zan: compiled together with it
// via ZANC_ARGS. Its `internal` Tag is reachable from Acc.Inner (and any
// namespace sharing that first-two-segment module) but not from Acc.Other.
using System;

namespace Acc.Inner;

class Core {
    internal static int Tag() { return 7; }
}
