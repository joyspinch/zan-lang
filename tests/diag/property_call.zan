using System;

namespace DiagPropertyCall;

/// `Count` is a property; spelling it with parentheses used to lower to a
/// constant 0 instead of failing to compile.
class Program {
    static void Main() {
        List<int> items = new List<int>();
        items.Add(1);
        Console.WriteLine(items.Count());
    }
}
