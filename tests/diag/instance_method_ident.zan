// A33-1: a bare instance method name used as a delegate value (`return Touch;`)
// must be a compile error, not a silent crash (same receiver-slot problem as
// the explicit `obj.Touch` form).
delegate void Action();
class Widget {
    public void Touch() { }
    public Action Make() {
        return Touch;
    }
}
