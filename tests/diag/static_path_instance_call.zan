// An instance method reached through the type name used to fall through to
// the static call path: the receiver argument was silently dropped and the
// program failed LLVM verification far from the call site (argument-count
// mismatch on every Ohos/OHOS-styled `Window.EventX()` typo). The static
// resolution path must reject non-static methods with a source diagnostic.
namespace Diag;
class Widget {
    int Draw() {
        return 7;
    }
}
class Entry {
    static int Main() {
        return Widget.Draw();
    }
}
