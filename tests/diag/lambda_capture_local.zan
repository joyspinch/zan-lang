// A33-1: a lambda that references an enclosing local must be a compile error,
// not a silent crash. Lambdas are non-capturing; captured locals used to be
// silently unresolved.
delegate void Action();
class Widget {
    public Action Make() {
        int n = 42;
        return () => { n = n + 1; };
    }
}
