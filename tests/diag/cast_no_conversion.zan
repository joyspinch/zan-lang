// Regression: a cast used to be the one expression the checker never looked
// at, so `(int)"abc"` compiled and reinterpreted the string's pointer bits.
using System;

class Program {
    static int Main() {
        int x = (int)"abc";
        return x;
    }
}
