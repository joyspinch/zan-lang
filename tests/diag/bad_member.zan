// Regression: a member using a reserved word (`value`) as its name once made
// the class-body parser spin forever (allocating until OOM). The parser must
// now recover, emit a bounded set of diagnostics, and exit non-zero promptly.
namespace Diag;
class Broken {
    public int value;
}
