// D6 negative regression: the invariant also applies to assignments and
// field initializers, not just call arguments.
class Box { int x; }
class P {
    static void Main() {
        List<int> v = new List<int>();
        v.Add(1);
        List<Box> b = v;
    }
}
