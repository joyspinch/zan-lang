class Point {
    int x;
    int y;
    int SumWith(int dx, int dy) {
        return x + dx + y + dy;
    }
    static int Main() {
        Point p = new Point();
        p.x = 3;
        p.y = 4;
        return p.SumWith(1, 2);
    }
}
