using System;

/* --publish scrambles every string literal in the image and un-scrambles it
 * from a startup constructor. The case only needs literals to exist: the test
 * inspects how that constructor is registered, per target. */
class Program {
    static void Main() {
        Console.WriteLine("hello world banner");
        Console.WriteLine("---- SECTION ----");
    }
}
