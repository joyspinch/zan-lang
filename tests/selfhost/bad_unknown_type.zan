// Fail-closed BADPROG for the selfhost harness: an unresolvable field type is
// a fundamental semantic error that gen1's checker must reject with a
// non-zero exit and no IR output. (The previous bad input, bad_member.zan,
// became legal code when `value` was made a contextual identifier — a member
// named `value` is now as valid as any other, so it no longer exercises the
// rejection path.)
class Broken {
    Nope field;
}

class Program {
    static void Main() { }
}
